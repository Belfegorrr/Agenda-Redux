class Contato {
  name: string
  email: string
  telefone: number
  id: number

  constructor(name: string, email: string, telefone: number, id: number) {
    this.name = name
    this.email = email
    this.telefone = telefone
    this.id = id
  }
}

export default Contato
